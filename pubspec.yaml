name: your_project_name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.3.0  # You can keep this version
  collection: ^1.18.0  # You can keep this version
  material_color_utilities: ^0.11.1  # You can keep this version
  meta: 1.15.0  # Change this line to match the SDK version

flutter:
  uses-material-design: true

  assets:
    - assets/img1.jfif
    - assets/img2.jfif
    - assets/img3.jfif
    - assets/background.jfif
    - assets/img5.jpg
